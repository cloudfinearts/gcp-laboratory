# warning, provider seems buggy and unfit for use, throws weird errors on this one
apiVersion: karpenter.k8s.gcp/v1alpha1
kind: GCENodeClass
metadata:
  name: default
spec:
  imageSelectorTerms:
    - alias: ContainerOptimizedOS@latest
  tags:
    env: dev
  disks:
    - category: pd-standard
      sizeGiB: 20
      boot: true